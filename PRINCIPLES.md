# L1/L2/L3 架构约定

## 核心原则

每一层只改自己负责的仓库，不越权：

- **L1** 修消费仓的应用代码
- **L2** 修流程仓的流程代码（L1 的实现）
- **L3** 修流程仓的 L2 基础设施

## 读写边界

| 层 | 可写 | 可读 | 禁写 |
|---|------|------|------|
| L1 | 消费仓代码（排除 `.github/workflows/`） | 消费仓 + 流程仓 | 消费仓 `.github/workflows/`、流程仓 |
| L2 | 流程仓（所有文件） | 消费仓 + 流程仓 | 消费仓（所有文件） |
| L3 | 流程仓 + 消费仓薄壳（极少） | 全部 | — |

## L1 限制

- 不能修改消费仓的 `.github/workflows/` 目录（薄壳）
- 不能修改流程仓的任何文件
- CI 配置需要调整时，在 PR 评论中说明，不直接改
- 消费仓的薄壳只定义触发条件和调用流程仓的 reusable workflow，L1 不碰

## L2 限制

- 不能修改消费仓的任何文件
- 不能向消费仓的 PR 分支推送 commit
- 可以修改流程仓的所有文件（L1 的 workflow、脚本、prompt template）
- 不能修改自己的逻辑代码（`capability-resolver.yml` 的 job 流程、触发条件）
- 可以修改自己的数据（`capability_gaps.py` 的模式库、prompt 内容）

## L2 触发条件

- 当前：L1 撞迭代上限时 dispatch L2
- 后续：L2 可通过修改 L1 代码（`fix.yml`）增加新触发条件
- L2 不修改自己的触发条件（那是 L3 的职责）

## L2 upgrade 能力

| 类型 | 说明 | 状态 |
|------|------|------|
| knowledge-append | 追加知识到 prompt template | 已实现 |
| design-fix | 修改 L1 的代码逻辑（fix.yml、fix_pr.py 等） | 待实现 |
| context-source-add | 添加上下文源到 L1 workflow | 待实现 |

## L3 职责

- 修 L2 的基础设施（权限、token、label、bash 语法）
- 增强 L2 的 upgrade 机制（实现新的 fix 类型）
- 加硬限制（L1 不改薄壳、L2 不改消费仓）
- 写约定（本文件）
- 监控全过程，记录问题

## 禁止事项

- L1 不能改 L1 自己的流程代码（那是 L2 的职责）
- L2 不能改 L2 自己的逻辑代码（那是 L3 的职责）
- L2 不能直接修消费仓的 PR（那是 L1 的职责）
- 任何层不能把自己的职责推给更低层
