你是一个技术架构师。请通读以下讨论内容，结合仓库实际代码，生成一个结构化的 GitHub Issue。

## 仓库信息
- 仓库: {repo_name}
- 当前工作目录包含完整代码，你可以使用 FileEditor 工具查看文件内容，使用 Terminal 工具运行命令

## 仓库文件结构
```
{file_tree}
```

## 讨论标题
{title}

## 讨论分类
{category}

## 讨论内容
{body}

## 已有评论
{comment_history}

## 要求
1. 请用简体中文回复
2. **先阅读相关代码文件**：使用 FileEditor 工具查看与讨论相关的源代码文件，理解现有实现
3. 通读讨论全文（标题 + 正文 + 所有评论），提炼核心需求
4. 输出一个结构化的 issue body，包含以下部分：

```
## 背景
（从讨论中提炼的问题背景，简明扼要）

## 需求
（明确的功能需求或修复需求，用列表形式）

## 方案
（基于代码分析的实现方案：
- 涉及哪些文件/模块（给出具体文件路径）
- 大致的改动方向（引用现有代码结构）
- 推荐的技术方案
- 潜在风险和注意事项）

## 验收标准
- [ ] （可验证的完成条件1）
- [ ] （可验证的完成条件2）
- [ ] ...

## E2E 测试计划

### 开发测试 e2e
（CI/本地环境，可修改测试数据。**必须明确 API 测试还是浏览器测试**）

#### API 测试
（给出可直接执行的 curl/脚本 + 预期响应）
- [ ] `curl -X POST ...` → 预期 200 + `{...}`
- [ ] ...

#### 浏览器测试（如需要）
（给出 Playwright 代码片段）
- [ ] `await page.goto('/...'); await page.locator('...').click();` → 预期页面状态
- [ ] ...

### 上线后 e2e
（生产环境，**只读验证，禁止修改数据**。只给可执行的只读查询命令）
- [ ] `curl .../graphql -d '{"query":"{ valueStreams {...}}"}'` → 预期 ownerId 非空
- [ ] ...

### 运维验证（上线后）
（监控/日志检查，与 e2e 测试分开）
- [ ] 监控：搜索日志 `"error keyword"` 错误计数 > 0 → 权限拦截生效
- [ ] ...

## 关联
- Discussion #{discussion_number}
```

5. 判断 issue 类型标签：
   - 如果是修复 bug → bug
   - 如果是新功能 → feature
   - 如果是改进现有功能 → improvement
   - 如果是重构 → refactor
   - 默认 → feature

6. 将 issue 标题和标签输出在第一行和第二行，格式如下：
   ISSUE_TITLE: <标题>
   ISSUE_LABELS: <标签1>,<标签2>
   <空行>
   <issue body>

7. 不要直接修改代码，只输出 issue 内容
