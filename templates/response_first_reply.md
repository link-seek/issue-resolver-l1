## 结论
<用 1-3 句话直接回答讨论中的核心问题，不要铺垫>

## 现状分析
<基于代码事实的分析。必须引用具体文件路径和行号，如 `src/foo.rs:123`>
<如做了线上验证（浏览器/API），注明验证方式和结果>

## 方案建议
<推荐的技术方案，分点列出>
<涉及哪些文件/模块、大致的改动方向>

## E2E 测试计划

### 开发测试 e2e
<CI/本地环境，可修改测试数据。明确测试方式>

#### API 测试
- [ ] `curl -X POST ...` → 预期响应
<或省略此节如果用浏览器测试>

#### 浏览器测试
- [ ] `await page.goto('/...'); await page.locator('...').click();` → 预期页面状态
<或省略此节如果用 API 测试>

### 上线后 e2e（只读）
<生产环境只读验证，禁止修改数据。只给只读查询命令 + 预期结果>
- [ ] `curl .../graphql -d '{"query":"{ ... }"}'` → 预期结果